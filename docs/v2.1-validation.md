# v2.1 Validation

This document is the release checklist for the `Safer Batch Downloads`
milestone. Automated checks should pass before the unpacked Chrome matrix is
started. Record the Chrome version and results in the final section.

## Automated Evidence

Run from the repository root with Codex's bundled Node runtime:

```powershell
$node = 'C:\Users\ckajs\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node --check download-utils.js
& $node --check popup-state.js
& $node --check popup.js
& $node --check background.js
& $node --test tests/*.test.js
Get-Content -Raw manifest.json | ConvertFrom-Json | Out-Null
git diff --check
```

The automated suite covers:

- aborting a 200-item queue, active download cancellation, retry suppression,
  late batches, late Chrome ids, duplicate events, and completion/abort races;
- service-worker restoration of active and pending downloads, including an
  indeterminate launch that must not be duplicated;
- starting a fresh batch after an aborted session;
- terminal selection that removes completed files and retains failed/cancelled
  files for retry;
- Pixiv 404 fallback across alternate original formats and the known master
  preview, including suppression of futile retries when every source is gone;
- destination traversal, stable thread folders, Windows reserved names,
  filename source priority, MIME extensions, route-name rejection, stable
  fallbacks, and deterministic duplicate suffixes;
- page-fetch controller cancellation;
- v2.0 progress and download-setting migration;
- selected/filter/ignore behavior, page-scoped saved state, confirmation
  threshold, and the popup DOM/script contract;
- compact candidate persistence, stale tab-state pruning, and on-demand
  page-preview retrieval without bulk base64 data in scan results.

## Permission Gate

`manifest.json` must retain the local-first permission set. Version `2.1` must
not add a backend, telemetry, accounts, cloud dependencies, multi-page crawling,
or broad host access. The only host permission remains:

```text
https://i.pximg.net/*
```

## Unpacked Chrome Matrix

Before testing, reload the unpacked extension at `chrome://extensions` and keep
Chrome's download prompt disabled for the extension.

| Flow | Steps | Pass criteria | Result |
| --- | --- | --- | --- |
| Direct media | Scan a page with direct JPG/PNG and video links; change filters and select a subset. | Counts update, previews stay stable, and only selected visible files download. | Pass |
| 4chan thread | Scan a media-heavy thread with thumbnails linked to originals. | Original dimensions and URLs are used; thumbnails remain previews; destination identifies the board/thread. | Pass |
| Forum attachments | Scan a signed-in forum with thumbnail attachment endpoints. | All candidates are visible; page-session fetches download; response or attachment filenames win when available. | Pass |
| Preview quota | Scan a forum page with hundreds of authenticated thumbnails and reopen the popup. | Scan completes without `Resource::kQuotaBytes`; previews load as needed; reopening does not produce a storage quota error. | Re-test: on-demand preview registry added after quota failure |
| Pixiv | Scan an artwork with original and related preview images. | Original dimensions/previews are correct and selected originals download with the narrow referer rule. | Re-test: 404 fallback added after initial failure |
| Large batch | Select at least 200 items and press Download. | One inline confirmation shows exact count, destination, and speed; no browser-dialog sequence appears. | Pass |
| Abort pending | Start the large batch and press Abort before most items launch. | Abort remains visible; queued count stops growing; pending items never start; cancelled count is shown. | Pass |
| Abort active | Abort while files and page attachments are active, then wait beyond the retry timeout. | Active work is cancelled where Chrome allows; no retries or late page batches restart the session. | Pass |
| Restart after abort | Start a small new batch after the aborted batch. | Completed files are removed from selection; Retry contains only failed/cancelled files and starts normally. | Re-test: terminal selection fixed after initial failure |
| Destination and names | Edit the subfolder with spaces, slashes, and `..`; download duplicate/generic attachment names. | Previewed path matches the actual path below `ImageDownloader/`; traversal is removed; names are useful and unique. | Pass |
| Upgrade state | Reload v2.1 over an existing v2.0 profile with saved candidates, filters, and ignore entries. | Existing page state remains usable and new destination/progress fields receive defaults. | Pass |
| Popup layout | Check normal width and a narrow popup with long host/name text and three-digit counts in ready, confirmation, downloading, and aborted states. | Popup opens at full height; no overlap or layout shift; filename truncation has a tooltip; footer and Abort stay reachable without result-list scrolling. | Re-test: explicit popup height added after collapsed-window failure |

## Browser Result

- Chrome version: Not recorded
- Initial tested commit: `6674e65`
- Date: 2026-07-19
- Blocking failures: Pixiv 404 fallback, terminal retry selection, footer action
  height, and bulk preview quota
- Notes: User supplied screenshots and results. Fixes require the three marked
  feature re-tests plus the new preview-quota test after reloading the
  extension.

The current Codex browser surface cannot load a local unpacked-extension page,
so the marked re-tests require a real Chrome extension context.
